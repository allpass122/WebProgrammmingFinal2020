{
  "name": "WebFinal",
  "homepage": "https://allpass122.github.io/WebProgrammmingFinal2020",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.21.1",
    "gh-pages": "^3.2.3",
    "styled-components": "^5.3.0"
  },
  "scripts": {
    "start": "cd frontend && yarn start",
    "server": "cd backend && yarn server",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
  }
}
